<project name="hello world cpp" default="jenkins" basedir=".">
    <description>
        Jenkins Ant file for hello world cpp
    </description>
  <!-- set global properties for this build -->
  <property name="app" location="hello world cpp"/>
  <property name="make.cmd" value="/usr/bin/make"/>
  <property name="build.native" value="./"/>
 
  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
  </target>
 
  <target name="compile"
        description="compile the source" >
    <exec dir="${build.native}" executable="${make.cmd}" failonerror="true">
    </exec>
  </target>
 
  <target name="clean"
        description="clean up" >
    <exec dir="${build.native}" executable="${make.cmd}" failonerror="true">
    <arg value="clean"/>
    </exec>
  </target>

</project>
