<cc>
     <shared value="jms" />
     <targetdir value="${target.dir}" />
     <tempdir value="${temp.dir}" />
     <include location="${source.dir}" />
     <include location="${source.generated.dir}" />
     <sources dir="${source.dir}" includes="*.cpp" />
     <sources dir="${source.generated.dir}" includes="*.cpp" />
     <define name="JMS_DECLSPEC" value="__declspec(dllexport)"
             os="win32,win64" />
     <define name="JMS_DECLSPEC" value=""
             notos="win32,win64" />
     <debug value="${debug.flag}" />
     <multithreaded />
     <dynamic />
     <rtti />
     <bool />
     <exceptions />
     <usecompilerdirectories />
</cc>
